# Optional: enables open-ended AI explanations of calculated inventory reports.
# Copy to .env.local, set your server-side key, then restart the Next.js server.
# Enable only after approving sending inventory reports to OpenAI.
INVENTORY_AI_ENABLED=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
