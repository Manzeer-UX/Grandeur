'use client';
import {useState} from 'react';
import {Filter,RotateCcw,Package,Network,ArrowUpRight,Download,Info} from 'lucide-react';
import {matchesArea,stockByProduct} from '../lib/stock-scope.mjs';
export function StockFilters({raw,area,distributor,onArea,onDistributor}:any){const regions=Array.from(new Set([...raw.warehouses,...raw.distributors].map(r=>r.region).filter(Boolean))) as string[];const choices=raw.distributors.filter((d:any)=>matchesArea(d,area));return <section className="stock-filters" aria-label="Stock scope filters"><div className="stock-filter-title"><Filter size={19}/><div><strong>Stock visibility</strong><small>Filter the cards, charts, tables, and exports below</small></div></div><label>Region<select aria-label="Stock region" value={area} onChange={e=>{onArea(e.target.value);onDistributor('')}}><option value="">All regions</option>{regions.map(r=><optgroup key={r} label={r}><option value={r}>All {r}</option>{Array.from(new Set([...raw.warehouses,...raw.distributors].filter((d:any)=>d.region===r).map((d:any)=>d.territory).filter((t:any)=>t&&t!==r))).map((t:any)=><option key={t} value={t}>{t}</option>)}</optgroup>)}</select></label><label>Distributor<select aria-label="Stock distributor" value={distributor} onChange={e=>onDistributor(e.target.value)}><option value="">All distributors</option>{choices.map((d:any)=><option key={d.id} value={d.id}>{d.name}</option>)}</select></label><button className="button" disabled={!area&&!distributor} onClick={()=>{onArea('');onDistributor('')}}><RotateCcw size={15}/>Clear filters</button><p className="stock-scope-note"><Info size={14}/>{distributor?'Distributor-held stock only. Shared warehouse inventory is excluded because it is not owned by this distributor.':`${area||'All regions'} · Warehouse stock + distributor-held stock. Sold stock is shown separately.`}</p></section>}
export function StockBreakdown({s,onDistributor,onExport}:any){const [view,setView]=useState('By product');const [query,setQuery]=useState('');const [index,setIndex]=useState(0);const products=stockByProduct(s);const rows=view==='By product'?products:s.distributors.map((d:any)=>({id:d.id,name:d.name,region:d.region,territory:d.territory,distributor:d.stock||0,sold:d.sold||0,flavors:d.positions?.length||0}));const filtered=rows.filter((r:any)=>JSON.stringify(r).toLowerCase().includes(query.toLowerCase()));const total=products.reduce((n:number,p:any)=>n+p.total,0);return <section className="panel stock-breakdown"><div className="panel-heading"><div><h2><Package size={19}/>Stock breakdown</h2><p><strong>{total.toLocaleString()}</strong> bottles across {s.warehouses.length} warehouses and {s.distributors.length} distributors · 320mL Bonko range</p></div><button className="button" onClick={()=>onExport(filtered,'filtered-stock')}><Download size={16}/>Export stock</button></div><div className="stock-view-tools"><div className="tabs" role="tablist">{['By product','By distributor'].map(t=><button role="tab" aria-selected={view===t} className={view===t?'active':''} key={t} onClick={()=>{setView(t);setIndex(0)}}>{t}</button>)}</div><input aria-label="Search stock breakdown" placeholder="Search stock…" value={query} onChange={e=>{setQuery(e.target.value);setIndex(0)}}/></div><div className="table-scroll"><table><thead><tr><th>{view==='By product'?'Product':'Distributor'}</th>{view==='By product'?<><th>Pack size</th><th>Warehouse stock</th></>:<><th>Area</th><th>Region</th></>}<th>Distributor stock</th><th>{view==='By product'?'Total stock':'Flavors'}</th><th>Sell-through</th>{view==='By distributor'&&<th/>}</tr></thead><tbody>{filtered.slice(index*10,(index+1)*10).map((r:any)=><tr key={r.id}><td><div className="product-label">{view==='By product'?<span className="stock-flavor" style={{background:r.color}}/>:<Network size={18}/>}<strong>{r.name}</strong></div></td>{view==='By product'?<><td>{r.size}</td><td>{r.warehouse.toLocaleString()}</td></>:<><td>{r.region}</td><td>{r.territory||'—'}</td></>}<td>{r.distributor.toLocaleString()}</td><td><strong>{(r.total??r.flavors).toLocaleString()}</strong></td><td>{r.sold.toLocaleString()}</td>{view==='By distributor'&&<td><button className="text-button" onClick={()=>onDistributor(r.id)}>View stock <ArrowUpRight size={15}/></button></td>}</tr>)}</tbody></table>{!filtered.length&&<div className="empty"><h3>No matching stock records</h3><p>Clear the search or choose another region or distributor.</p></div>}</div><div className="table-footer"><span>{filtered.length} records · Workspace balances</span><div className="pagination"><button className="text-button" disabled={index===0} onClick={()=>setIndex(index-1)}>Previous</button><span>{index+1} / {Math.max(1,Math.ceil(filtered.length/10))}</span><button className="text-button" disabled={(index+1)*10>=filtered.length} onClick={()=>setIndex(index+1)}>Next</button></div></div></section>}
export function ScreenshotReference({s}:any){const ref=s.demo?.reference;if(!ref)return null;return <details className="panel source-reference"><summary>Screenshot reference · sales figures and catalog details <span>View source notes</span></summary><div className="notice amber-notice"><Info size={18}/><p>The STT heading is 34,777; its nine bars sum to 34,437 (difference: 340). Region V appears twice; Region XI is absent. These reference figures do not change when filtering workspace stock. Screenshot chart units are unspecified.</p></div><div className="charts-grid"><div className="table-scroll"><table><thead><tr><th>STT source label</th><th>Source value</th></tr></thead><tbody>{ref.sttBars.map(([name,value]:any)=><tr key={name}><td>{name}</td><td>{value.toLocaleString()}</td></tr>)}</tbody></table></div><div className="table-scroll"><table><thead><tr><th>STD flavor</th><th>Source value</th></tr></thead><tbody>{ref.std.map((r:any)=><tr key={r.name}><td>{r.name}</td><td>{r.value.toLocaleString()}</td></tr>)}</tbody></table><p className="chart-caption">SKU screenshot: 320mL · SRP ₱1,096.20 / case and ₱45.68 / piece. Case quantity is not specified in the screenshot; no case-size conversion is inferred from these rounded prices.</p></div></div></details>}


